// Executor
process {
    executor = "local"
}

// QueueSize limit
if (params.parallel_jobs || params.parallel_jobs != '') {
    qs = params.parallel_jobs
}
executor {
    name = "local"
    if (params.parallel_jobs || params.parallel_jobs != '') {
    queueSize = qs
    }
}

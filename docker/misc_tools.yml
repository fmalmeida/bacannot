# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: bacannot-miscellaneous

channels:
  - conda-forge
  - bioconda
  - defaults
  - anaconda

dependencies:

  ## pip dependecies
  - pip
  - pip:
    - docopt
    - pandas


  ## conda-forge packages
  - conda-forge::curl

  ## anaconda packages
  - anaconda::git

  ## bioconda packages
  - bioconda::blast=2.12.0
  - bioconda::diamond=2.0.13
  - bioconda::bedtools=2.30
  - bioconda::samtools=1.14
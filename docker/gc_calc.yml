# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: bacannot-gc-calc

channels:
  - conda-forge
  - bioconda
  - defaults
  - anaconda

dependencies:

  ## bioconda packages
  - bioconda::bedtools=2.30
  - bioconda::samtools=1.14
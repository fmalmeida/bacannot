# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: bacannot-python

channels:
  - conda-forge
  - bioconda
  - defaults
  - anaconda
  - falmeida

dependencies:

  ## bioconda packages
  - bioconda::plasmidfinder
  - bioconda::platon
